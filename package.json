{
  "name": "huey",
  "version": "1.0.0",
  "description": "Create a color range based off a base color that maintains the same saturation and lightness",
  "keywords": [],
  "main": "src/index.js",
  "dependencies": {
    "color-convert": "1.9.2",
    "randomcolor": "0.5.3",
    "react": "16.3.2",
    "react-dom": "16.3.2",
    "react-scripts": "1.1.4"
  },
  "devDependencies": {
    "eslint": "^4.19.1",
    "eslint-config-prettier": "^3.0.1",
    "eslint-plugin-prettier": "^2.6.2",
    "eslint-plugin-react": "^7.11.1",
    "prettier": "1.14.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "deploy": "react-scripts build && aws s3 cp --recursive ./build s3://huey.theirongiant.io",
    "eject": "react-scripts eject"
  }
}
